# lms-file-tracker
The University LMS File Tracker is a system that manages file uploads with version control. It stores each update as a new version instead of overwriting files, allowing users to track changes and access previous versions. This improves organization, prevents data loss, and simplifies file management in an LMS.
